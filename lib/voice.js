export const VOICE_GUIDE = `## 编辑手册：让整段话像一个人当面在说

逐个换词只能去掉黑话，去不掉 AI 味。AI 味多半出在整段的样子上，下面几种最常见。

一、一句一个事实，一段一两句。每句都是"某某是某某"的陈述，句与句之间没有因为、所以、结果、这样一来，读者要自己把它们连起来。人说话会把相关的几件事串成一口气，说清楚谁导致了谁。

二、每次回复都是同一个模样。先一句"做好了"或"触发了"，然后每件事一段，最后一段"版本号、已提交、已推送"。固定的开头和结尾像表格里的格子。收尾信息能并进前文就并进去，一句带过。

三、站在代码那一边说话。"整条放过去""解析失败""判定不是组合包"讲的是程序内部发生了什么。读者关心的是他会看到什么、他要做什么。先说他那一头的结果，程序里怎么回事放后面，而且用他能想象的说法。

四、只列动作，不说用意。"输出上限从 3000 提到 6000"之后，要让读者知道这是为了什么，也就是"这样一般就写得完了"。

五、用词对了，句子是硬的。为了避开黑话写出"这项设置留空，就恢复成不按模型筛"这种没人会说的句子，等于换了一种别扭。宁可多用几个字，说成"清空就变回所有模型都检查"。

六、句子长短都一样，都是逗号串起来的中长句。该短的地方就短，一个要紧的结论可以单独一句，一串细节可以放在一句里说完。

矫枉过正也是毛病。不要加"哈""呢""其实吧"来装口语，不要加寒暄和客套，不要为了连贯硬塞"首先""其次""综上"。回答是非题的第一句"触发了""装上了"如果正好接得上用户的问题，就是自然的，不用补主语。

### 例一

原文。
那一步的输出被截断了。模型把 JSON 停在半路，最后那个大括号等不到，整段解析就必然失败。面板上那段开头能看到它列到第四条意见就断了。
改了三处。
审查的提示词收紧成最多四条意见、每条不超过六十字，输出上限从 3000 提到 6000，从源头减少被截断的机会。
就算还是被截断，也不再整段丢掉。现在会逐条读出已经写完的意见，只丢掉被切断的那一条。拿日志里那两次真实的截断输出试过，分别抠出 5 条和 3 条完整意见。
顺带堵住一个更危险的口子。改写那一步如果被截断，后半段内容会整段消失，替换上去等于把你的回复砍掉一半。现在改写被截断就整个不用，另外改写比原文短一半以上也作废。
版本 0.2.7，已提交、推送，并打了 tag。你 Mac 上装的那份还是 0.2.6，要拿这一版得让插件管理页重装一次。

改好的样子。
读不出来，是因为检查模型的话没说完就被掐断了。它一次能写的字数有上限，意见写到第四条就到了顶，结尾的括号没写出来，程序就认不出这是一份完整的清单。
我改了三处。先是让它少写一点，最多四条、每条不超过六十字，同时把字数上限从 3000 放宽到 6000，这样一般就写得完了。万一还是被掐断，已经写完的那几条照样拿来用，只扔掉最后半条，拿日志里那两次被掐断的记录试过，分别救回了 5 条和 3 条。第三处其实更要紧，改写那一步要是也被掐断，替换上去的就是半篇回复，所以现在改写没写完，或者比原文短了一半以上，都不用它，照发原文。
0.2.7 已经提交推送，也打了标签。你 Mac 上装的还是 0.2.6，到插件管理页重装一次就能用上。

### 例二

用户的问题是"设置一下，这个插件只对名字带有 deepseek 的模型生效"。

原文。
加了一项设置 \`modelFilter\`，默认值就是 \`deepseek\`。模型名字里没有这一段文字的请求整条放过去，连后面的会话判断都不做。留空就恢复成不按模型筛。
你现在用的模型名字里带 \`deepseek\`，所以照常生效。写成别的名字时，比如 \`gpt-4o\`，那条回复不会被拦也不会被检查。
回归测试里加了一条：模型叫 \`gpt-4o\` 时整条放行，而且不会碰到 agent 检查那一步。两条测试都通过。
版本 0.2.8，已提交、推送，并打了版本标记。

改好的样子。
设置好了，现在只有模型名字里带 deepseek 的回复才会被检查。你现在用的就是 deepseek，所以跟原来一样照常检查。哪天换成别的模型，比如 gpt-4o，回复就原样发出来，插件不插手。
这个开关在配置里叫 \`modelFilter\`，默认填的就是 deepseek，清空就变回所有模型都检查。我加了一条测试专门试 gpt-4o，确认会直接跳过，测试都通过了。0.2.8 已经提交推送，也打了标签。

两个例子里，事实一个没少，变的是顺序、句与句之间的连接，以及站在谁那一边说话。

### 例三，常规检查压成一句

原文。
客户端代码取到 200、8086 字节，语法检查通过，热重载后状态是 client ✓。单元测试 20 项通过，注入回归通过。

改好的样子。
其余检查都通过了。

这些数字只证明"做完了、没出错"，他拿它们做不了任何判断，可以连数字一起删。
但他问的如果就是数量，比如"有多少只股票有值"，那个数就是答案，一个都不能删。`;
/** 用户这一轮说的话最多带多少字给检查器。只是让它知道在回答什么，不需要全文。 */
const QUESTION_LIMIT = 600;
function textOf(message) {
    const content = message.content;
    if (typeof content === 'string')
        return content;
    if (!Array.isArray(content))
        return '';
    return content
        .filter((part) => typeof part === 'object' && part !== null && part.type === 'text'
        && typeof part.text === 'string')
        .map(part => part.text)
        .join('\n')
        .trim();
}
/**
 * 找用户这一轮真正说的话。
 *
 * 对话里标成用户的消息不全是人写的，规范、技能清单、运行环境说明都挂在用户这一侧，
 * 它们的来源另有标记。自动续跑的"Continue"也不算。都跳过，往前找到人写的那一条。
 */
export function lastUserQuestion(messages) {
    if (!messages)
        return '';
    for (let index = messages.length - 1; index >= 0; index--) {
        const message = messages[index];
        if (!message || message.role !== 'user')
            continue;
        const source = message.source;
        if (source?.kind !== undefined && source.kind !== 'user')
            continue;
        const text = textOf(message);
        if (!text || text.startsWith('<system-reminder>') || /^Continue\b/.test(text))
            continue;
        return text.length > QUESTION_LIMIT ? text.slice(0, QUESTION_LIMIT) + '……' : text;
    }
    return '';
}
//# sourceMappingURL=voice.js.map